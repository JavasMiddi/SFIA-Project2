- name: Install NGINX File
  template:
    src: ~/SFIA-Project2/nginx/nginx.conf
    dest: /etc/nginx/nginx.conf
    register: nginx_config

- name: Export New Config
  add_host:
    name: "new_config"
    value: "{{ nginx_config }}"
